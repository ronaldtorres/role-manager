import Vue from "vue";
import Vuex from "vuex";
import roles from "./roles.store";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    roles,
  },
});
